# Decision Log

Журнал ключових рішень. Активні відкриті питання — у `activeContext.md`.

## D-001 — Async-перший SQLAlchemy

- **Рішення:** усі DAO і таски використовують `AsyncSession`. Синхронний engine
  (`sync_sessin`) лишено тільки для викликів усередині `event.listen`-хуків
  (`SyncTaskService.match_task`).
- **Причина:** `Jira`/`Tempo` API повільні; async-IO дозволяє паралелити
  заван­таження без додаткових тредів.
- **Альтернативи:** повністю синхронний код (відкинули — не масштабується),
  threading (відкинули — складніше керувати сесіями SQLAlchemy).
- **Наслідки:** треба обережно з event-листенерами SQLAlchemy — там не можна
  робити `await`, тому `SyncTaskService` має окремий sync-шлях.

## D-002 — DTO через Pydantic v2

- **Рішення:** окремі DTO-моделі для кожного зовнішнього сервісу
  (`services/<name>/dto.py`), `Config.from_attributes = True`.
- **Причина:** валідація і нормалізація даних від зовнішніх API виконується
  до запису в БД; ID-валідатори `≤ 0 → None` дають uniform-нульові FK.
- **Альтернативи:** `dataclasses` (відкинули — нема валідації), словники
  (відкинули — нема типізації).

## D-003 — Локальний кеш ключів `JRProject` / `KeyTemplate`

- **Рішення:** `SyncTaskService` тримає класовий кеш зі `cache_ttl = 2 год`.
- **Причина:** `event.listen('set', ...)` на `TCEntry.description` викликається
  багато разів при пакетному імпорті — кожен SELECT був би надлишковим.
- **Наслідки:** після додавання нового `JRProject` через UI/SQL до 2 годин
  triggers можуть не бачити його. Обхід — рестарт або примусове скидання
  `SyncTaskService.cache_timestamp = None`.

## D-004 — `Computed` для `tc_entries.duration`

- **Рішення:** колонка `duration` оголошена як `Integer Computed("EXTRACT(EPOCH
  FROM end_at - start_at)")`.
- **Причина:** уникнути неконсистентності між фактичним інтервалом і збереженим
  `duration` (раніше це поле заповнював TimeCamp і воно іноді розходилося).
- **Наслідки:** при INSERT/UPDATE `duration` не передається з коду; будь-який
  міграційний рух Postgres-функції `EXTRACT` має зберігатись.

## D-005 — `BaseDAO._sync` видаляє відсутні моделі

- **Рішення:** `sync_all` — full-replace: моделі з БД, відсутні у вхідному
  DTO-списку, **видаляються**.
- **Причина:** для словникових сутностей (`JRProject`, `TCProject`) це
  природна повна синхронізація з джерелом.
- **Наслідки:** для worklog-ів і entries обов'язково використовувати
  `sync_all_between(...)`, який обмежує вибірку періодом. Не можна викликати
  `sync_all(db, [...])` із частковим списком worklog-ів — це знищить решту.

## D-006 — Стейт-машина статусів `WorklogSyncTask`

- **Рішення:** окрема таблиця `worklog_sync_tasks` із enum-статусами замість
  прямого створення worklog-у з entry.
- **Причина:** треба resumability — якщо `Tempo` API впав посеред пачки,
  можна продовжити з того самого статусу. Також стейт `pre_create → create`
  дозволяє підтягнути відсутні `JRIssue` окремим кроком.
- **Альтернативи:** робити все в одному виклику (відкинули — крихкість при
  частковому збої).
- **Зарезервовано:** `pre_update/update/updated/sync` — на майбутнє для
  оновлення вже синхронізованих worklog-ів (поки без реалізації).

## D-007 — `FastAPI` як майбутній HTTP-шар

- **Рішення:** залишити `fastapi` і `uvicorn` у залежностях, але не піднімати
  HTTP-сервер у v0.1.
- **Причина:** проект — single-user CLI/notebook. HTTP знадобиться, коли
  з'являться інші юзери або зовнішні триггери (cron, webhook).
- **Поточний обхід:** запуск через `main.py` або клітинки `main.ipynb`.

## D-008 — Typo `worllog` у назвах файлу і класу

- **Рішення:** залишити `app/tasks/worllog_sync_task.py` і клас `WorllogSyncTask`
  без перейменування.
- **Причина:** перейменування зачепить імпорти у `main.ipynb` та потенційно
  у скриптах оператора. Користі мало, ризик регресії — є.
- **Перегляд:** коли з'явиться окрема задача на cleanup — перейменувати разом
  із пошуком усіх `worllog` у репо (на 2026-05-11 знайдено лише цей файл).

## D-010 — `api_jobs` із проміжним станом `needs_verification`

- **Рішення:** кожен `POST /sync/**` створює рядок у `api_jobs`. Сихронізація
  вважається **завершеною** не одразу після `200 OK`, а лише після
  `POST /api-jobs/{id}/verify` із валідного токена. Перехід — атомарно в
  `APIJobDAO.mark_verified`; verify на `failed`/`verified` → `409`.
- **Причина:** sync пише в зовнішні системи (`Jira`, `Tempo`). Між
  «технічний успіх» і «дані виглядають коректно» — є зазор; цей зазор
  закриває людина. Плюс це повноцінний audit-log (хто запустив, з якими
  параметрами, який був `result`), доступний місяцями через звичайний
  SELECT.
- **Альтернативи:** *Celery + Redis* (відкинули — Celery вирішує «як
  виконати», не «що записати і чи людина прийняла»; додатковий брокер
  зайвий для однокористувача). *Тільки sync без api_jobs* (відкинули —
  немає аудиту, не можна перевірити синхронізацію постфактум).
- **Наслідки:** `jobs_wrapper.run_job` тримає **окрему сесію** для INSERT/UPDATE
  audit-row — щоб журнал лишався цілим навіть якщо основна транзакція
  request-сесії відкочується. Накопичені `needs_verification` рядки треба
  буде чистити окремою майбутньою зміною `add-api-jobs-cleanup`.
- **Деталі реалізації:** D3 і D8 у
  [`design.md`](../../openspec/changes/archive/2026-06-12-add-rest-api/design.md);
  `app/api/jobs_wrapper.py`; `app/dao/api_job_dao.py`.

## D-009 — Multi-user-ready схема API з самого початку

- **Рішення:** на етапі `add-rest-api` (фактично single-user) уже
  закладаємо таблицю `api_users` із `password_hash`, `worker_key`,
  `is_active` і кладемо `worker_key` у JWT claim. Жодних харкодів
  `current_user` на HTTP-шарі. `settings.current_user` лишається
  fallback-ом лише для CLI/notebook-сценаріїв.
- **Причина:** roadmap (див. `projectbrief.md`) передбачає масштаб
  4–100 користувачів на пізніших етапах. Перейти від «один користувач у
  `.env`» на таблицю користувачів пізніше — це міграція, переписаний
  auth і ламані контракти API. Зробити це одразу — майже безкоштовно.
- **Альтернативи:** *env-bcrypt single user* (відкинули — погана дорога
  на multi-user, рефакторинг неминучий). *OAuth/Keycloak* (відкинули —
  overkill і зараз, і пізніше для внутрішнього сервісу).
- **Наслідки:** першого користувача спершу заводили ручним `INSERT`; із
  2026-06-12 для цього є CLI-команда `python -m app.cli add_user`
  (`app/cli/`). Повноцінне керування користувачами піде окремою зміною
  `add-user-management-cli`. Per-user OAuth-токени на `Jira`/`TimeCamp`
  теж відкладаємо в окрему майбутню зміну.
- **Деталі реалізації:** D1 у `design.md` (заархівовано в
  `openspec/changes/archive/2026-06-12-add-rest-api/`) і канонічний
  `openspec/specs/api-auth/spec.md`.
- **Джерело:** діалог 2026-05-12 із підтвердженням roadmap (multi-user
  до 4–100 користувачів).

## D-011 — Прямий `bcrypt` замість `passlib`

- **Рішення:** хешування паролів `api_users.password_hash` робиться
  безпосередньо через бібліотеку `bcrypt` у `app/api/auth.py`
  (`hash_password`/`verify_password`); `passlib` прибрано із залежностей.
- **Причина:** `passlib` 1.7.4 (останній реліз 2020) несумісний із
  `bcrypt` 5.x на Python 3.14 — падає на читанні версії бекенду
  (`module 'bcrypt' has no attribute '__about__'`) і кидає на 72-байтовому
  ліміті. Пінити старий `bcrypt` — тупикова дорога (немає коліс під 3.14).
- **Альтернативи:** *пін `bcrypt<4.1`* (відкинули — крихко, без wheels під
  3.14); *залишити `passlib`* (відкинули — фактично unmaintained).
- **Наслідки:** формат хешу лишився стандартним `$2b$`, тож наявні хеші й
  логін сумісні. 72-байтовий ліміт bcrypt обробляється явно й однаково для
  hash і verify (довгі секрети усікаються до 72 байтів). Прямий `bcrypt`
  використовує і CLI-команда `add_user`.
- **Джерело:** сесія 2026-06-12 (помилка при першому запуску `add_user`).

## D-012 — Монорепо `api/` + `front/`, пакет `src`→`app`, легкі per-folder вказівники

- **Рішення:** репозиторій розділено на `api/` (Python-бекенд) і `front/`
  (Vue 3 + Vite + TS). Пакет бекенду перейменовано `src` → `app` (фізично
  `api/app/`), усі ~90 імпортів переписано. Точки входу, `migrations/`,
  `alembic.ini`, `pyproject.toml`/`uv.lock`, `.python-version`, ноутбуки —
  у `api/`. `Makefile`, Docker, `docs/`, `openspec/` лишилися на корені;
  `docker-compose.yml` став мультисервісним (`api` context `./api`, новий
  `front` сервіс, спільна мережа, dev-проксі Vite на `http://api:8000`).
- **Причина:** roadmap веде до web-інтерфейсу; розділення дає кожній частині
  свій стек, тулчейн і інструкції. `app` — конвенційне ім'я кореневого
  пакета FastAPI-сервісу, прибирає двозначність `src` у монорепо з
  `front/src`.
- **Альтернативи:** лишити пакет `src` під `api/` (відкинули — закріплює
  неінформативну назву, плутає з `front/src`); фронт окремим репозиторієм
  (відкинули — спільні Memory Bank/OpenSpec/Docker зручніше тримати разом).
- **Зміна конвенції скілів:** попереднє правило «скіли глобальні, локальних
  копій не тримаємо» свідомо переглянуто. Тепер `api/` і `front/` мають
  власні `AGENTS.md`/`CLAUDE.md` з **легкими вказівниками** на релевантні
  глобальні скіли + стек-специфіку; повноцінних `.claude/skills/` у підтеках
  не заводимо. Загальні скіли лишаються спільними на корені. Причина —
  per-folder орієнтація без дублювання й підтримки скіл-пакетів у кожній теці.
- **Наслідки:** усі бекенд-команди тепер виконуються з `api/` (кореневий
  `Makefile` робить `cd api && …`); фронт-команди — з `front/`. HTTP-клієнт
  фронту — на `fetch` (без `axios`), стейт — Pinia. Доменна логіка, моделі та
  схема БД не змінювалися (alembic head лишився `ef2c7288bbb0`).
- **Джерело:** зміна `restructure-monorepo-frontend` (заархівована
  2026-06-12; proposal/design/specs у
  `openspec/changes/archive/2026-06-12-restructure-monorepo-frontend/`).
  Host-порти продукту — `10331/10332/11331` (схема `TT AA S`, скіл
  `preferred-docker-images`).

## D-013 — Google-вхід: серверна верифікація, match-by-email, єдиний JWT

- **Рішення:** додано `POST /auth/google` — альтернативний спосіб довести
  особу через Google, що видає **той самий** наш JWT, що й логін/пароль (claims
  `sub`, `user_id`, `worker_key`, `iat`, `exp`). Фронт шле або `{credential}`
  (One Tap / GIS ID-token), або `{code}` (popup auth-code, бекенд обмінює в
  Google через `redirect_uri=postmessage`). Бекенд **серверно** верифікує
  ID-token (`google-auth`: підпис JWKS, `aud == GOOGLE_CLIENT_ID`, `iss`, `exp`,
  `email_verified`), дістає e-mail і шукає **активного** `api_users` за `email`
  (lower-case). Невідомий/неактивний e-mail або будь-яка невдача верифікації →
  однакова `401 "account not found"`; **авто-реєстрації немає** (реєстрацію
  робить адмін). Нова колонка `api_users.email` (`UNIQUE`, nullable; ревізія
  `c03728fbb1cf`).
- **Причина:** Google — лише ще один спосіб входу; решта застосунку
  (auth-gate, refresh, `worker_key` для синку) уже працює на нашому JWT, тож
  після входу сесія однакова. Клієнтському e-mail не довіряємо — підпис
  перевіряється на беку.
- **Обидві гілки в скоупі (не опційні):** мають працювати всі три способи
  входу, тому `GOOGLE_CLIENT_SECRET` **обовʼязковий** для `code`-обміну і живе
  **лише** в backend-env (не у фронт-бандлі). Порожній `GOOGLE_CLIENT_ID` →
  `503` (Google-вхід вимкнено), `{code}` без secret → `503`; логін/пароль
  працює незалежно.
- **Домени/розподіл env:** фронт отримує лише публічний `VITE_GOOGLE_CLIENT_ID`;
  authorized JS origin для One Tap/popup — `https://sync.dev` (HTTPS-вимога
  Google); CORS для власних запитів не потрібен (один origin за host-nginx/
  Vite-проксі).
- **Альтернативи:** повний server-side redirect OAuth (відкинули — користувач
  хоче popup + One Tap, фронт-центричний флоу); авто-провіжн нового юзера
  (відкинули — суперечить «реєстрацію робить адмін»); cookie-сесія замість
  Bearer-JWT (відкинули на цей етап).
- **Джерело:** зміна `add-web-ui-foundation` (фаза 1 веб-UI). Capability
  `api-google-auth` + frontend `web-auth`. Деталі — `design.md` зміни (D1–D2,
  D8).

## D-014 — Front HTTP-клієнт: authed-by-default + токен через DI-provider

- **Рішення:** `front/src/api/client.ts` авторизує запити **за замовчуванням**;
  публічні ендпоінти явно опт-аутяться `{ public: true }` (`/healthz`,
  `/auth/login`, `/auth/google`). Токен клієнт бере не зі storage, а через
  зареєстрований `tokenProvider()` (інверсія залежностей, як `onUnauthorized`);
  у `main.ts` це `() => auth.token` — **жива Pinia-ref**, тобто єдине джерело
  істини в пам'яті. Захищений запит без токена не йде на сервер (одразу
  локальний `401` + `onUnauthorized`). Персист у `localStorage`
  (`useStored`/`lib/storage`) лишається, але вже `flush:'sync'` і **не в
  критичному read-шляху** — потрібен лише для відновлення сесії після reload.
- **Причина:** початково клієнт читав токен зі storage, куди store писав його
  асинхронно через `watch` (`flush:'pre'`). Через мікротаск-затримку
  `login()`→одразу `fetchMe()` бачив у storage ще старе значення → `/auth/me`
  ішов без `Authorization` → `401 "Not authenticated"`. Замість латання
  таймінгу прибрано сам клас проблеми: read і write — один об'єкт у пам'яті,
  гонці нема де виникнути.
- **Чому authed-by-default:** новий ендпоінт (попереду calendar/timesheet/
  data-screens) неможливо випадково лишити без `Authorization` — забути не
  можна, можна лише свідомо опт-аутнути публічний.
- **Контракт spec не змінився:** вимога `web-auth` «зберігати JWT через
  storage-обгортку і прикладати `Authorization: Bearer` до захищених запитів»
  лишається істинною; це суто деталь реалізації (тому правок дельта-специфікацій
  не робили).
- **Альтернативи:** *лише `flush:'sync'`* (відкинули — лікує симптом, не клас);
  *прямий імпорт store у client* (відкинули — циклічна залежність; DI-хук її
  уникає).
- **Джерело:** сесія 2026-06-13 (баг `/auth/me` 401 після успішного login),
  рефактор у рамках закриття `add-web-ui-foundation`.

## D-015 — Users CRUD: invite без пароля, `username` як ім'я, RBAC/matching відкладено

- **Рішення (фаза 3, `add-data-screens`):** додано CRUD користувачів
  (`GET/POST/PATCH/DELETE /users`). `POST /users` створює акаунт **без пароля**
  (`password_hash = NULL`) — вхід через Google за `email` (узгоджено з D-013).
  Єдина зміна схеми — `api_users.password_hash → nullable` (alembic head
  `10b7dc50b00f`); логін/пароль на NULL-хеш дає `401` (перевірка до
  `verify_password`).
- **Відображуване ім'я = наявний `username`** (рішення користувача): окрему
  колонку `name` **не додаємо**. `username` уже `NOT NULL`/`UNIQUE` і вільний для
  invite-користувачів (вони не входять логіном/паролем). Наслідок: ім'я
  унікальне (дубль → `409`, як і e-mail). Форма «ім'я» мапиться на `username`.
- **RBAC відкладено (D2 design):** селектор ролі у формі показано, але роль
  **не персиститься/не енфорситься**; колонку `role` не додаємо. Усі
  `/users`-endpoint-и доступні будь-якому авторизованому (single-user
  адмін-контекст). Енфорс — майбутня `add-rbac`.
- **Untracked → issue matching відкладено:** кнопка «Зіставити» показана, але
  вимкнена; майбутня `add-untracked-matching`.
- **`last_seen` відкладено** (рішення користувача): трекінгу останнього входу не
  додаємо; у таблиці користувачів — «—». Майбутня окрема зміна.
- **«Синхронізувати вибрані» (Tempo) = period-based** (рішення користувача,
  Q1): мапиться на наявний `push-to-tempo` за період; точковий `ids[]` —
  відкладено.
- **Jira-read (`api-jira-read`):** `GET /jr-issues` читає локальні `jr_issues`
  (наповнюються наявним `UpdateJiraTask`); `PATCH /jr-projects/{id}` міняє лише
  локальний `is_watched`. У Jira нічого не пишемо.
- **Побічний фікс конфігу:** `app/config.py` отримав `extra="ignore"` —
  спільний `<root>/.env` (його ж читає docker-compose) легітимно містить не-`APP__`
  ключі (`VITE_*`), на яких backend `Settings` раніше падав (`extra_forbidden`)
  при локальному запуску alembic/API. У контейнері проблема не виявлялась (файл
  за абсолютним шляхом відсутній, беруться лише compose-env `APP__*`).
- **Альтернативи:** *додати колонку `name`* (відкинули — `username` достатньо);
  *генерувати тимчасовий пароль і слати лист* (відкинули — нема поштового
  тракту, Google-вхід уже є); *одразу `ids[]` у push-тригер* (відкинули —
  розширення `api-sync-triggers` поза скоупом v1).
- **QA-рефінмент (2026-06-13, за фідбеком користувача):**
  - **Періоди дані-екранів розділено** (`front/src/lib/period.ts`): читання —
    `reviewPeriod()` (останні 6 міс, щоб дані показувались самі при відкритті,
    а не лише за поточний місяць — у БД worklog-задачі закінчувались травнем,
    тож Tempo за «червень» був порожній); sync-тригери — `syncPeriod()`
    (поточний місяць, щоб не тягнути забагато з зовнішніх API за клік).
  - **TimeCamp/Jira: авто-синк при відкритті** замість кнопок «тягнути».
    Екран показує БД одразу, потім у фоні `sync` із зовнішнього джерела +
    reload. Вікно свіжості 5 хв (кожен синк створює `api_jobs`
    `needs_verification`, тож не флудимо на кожну навігацію). **Tempo-конвеєр
    (prepare/resolve/push) лишається ручним** — він мутує Tempo, авто
    запускати небезпечно. Spec `frontend-data-tables` оновлено під це.
  - **Tempo-зведення — без `failed`:** worklog-задачі мають лише
    `pre_create/create/created` (StatusTaskEnum), статусу `failed` у них не буває
    (це api-job статус). Прототип показував `failed` помилково.
- **Джерело:** сесія 2026-06-13, реалізація `add-data-screens` (3 відкриті
  питання design.md закриті рішеннями користувача) + браузерний QA-рефінмент.

## D-016 — Календар (фаза 2): read-only візуалізація; стратегія «спочатку візуалізувати, потім по цеглинці»

- **Рішення (фаза 2, `add-calendar-timesheet`):** скоуп зміни звужено з
  початкового «повне редагування блоків + запис на бекенд» до **read-only
  візуалізації**. Лишається лише `GET /calendar` (read поверх наявних
  `tc_entries`/`worklog_sync_tasks`/`tc_projects`, **без alembic-міграції і нових
  колонок**) і фронт-екран тижня (сітка, блоки 3 варіанти, стани синку
  `service`/`tempo`/`synced`, тулбар, фільтри, навігація, тоталі, панель деталей
  у режимі перегляду).
- **Ширша стратегія фронту:** спершу **візуалізувати** все, що вже є в БД
  (read-only екрани), потім проходитись по кожній сторінці окремо «по цеглинці»
  — додавати редагування/запис на бекенд окремими змінами. Календар — перший
  приклад; так найскладніша частина (запис у Tempo) ізолюється від безризикового
  read-шляху.
- **Кінцева ціль збережена як майбутні зміни (щоб не загубити):**
  - `add-calendar-editing` — колонка `worklog_sync_tasks.billable`
    (+ опц. `is_manual`; увага: `WST.source_id` зараз `NOT NULL`, для ручних
    блоків треба nullable/`is_manual`), CRUD блоків
    (create/edit/delete/split/duplicate), drag/resize, per-block і масовий sync
    через наявний `resolve-issues`+`push-to-tempo`.
  - `add-worklog-update-flow` — round-trip update/delete вже-`synced` worklog-ів
    у Tempo через активацію зарезервованих `pre_update→update→updated`
    (`StatusTaskEnum`), семантика дубль-`synced`. **Закриє** відкрите питання
    «сценарій оновлення worklog-ів» (поки лишається відкритим — переноситься
    сюди).
  - Явний `project_key`/FK у WST для надійного кольору (зараз колір деривується
    з `tc_project.color`/префікса `issue_key`) — відкладено.
- **Уточнення по даних (узгоджено з D-015):** у `StatusTaskEnum` **немає**
  статусу `failed` (це стан `api_jobs`, не worklog-таска), тож read-бекенд
  календаря `failed` не віддає; `failed` лишається **суто фронтовим візуальним
  станом** (forward-compat) для майбутньої sync-фази.
- **Альтернативи:** *робити повне редагування одразу* (відкинули — найбільший і
  найризикованіший шматок, запис у Tempo, затримав би цілісну візуалізацію);
  *окрема таблиця `calendar_blocks`* (відкинули на цей етап — більше коду; має
  сенс із власним трекером, roadmap stage 3).
- **Статус:** **реалізовано і заархівовано 2026-06-13** (браузерний QA пройдено
  наживо). 2 нові capability злиті в `openspec/specs/` і канонічні:
  `api-calendar` (read `GET /calendar`), `frontend-calendar` (екран тижня).
  Стан блоку деривується в роутері (`service`/`tempo`/`synced`; `failed` не
  віддається). Без alembic-міграції (head лишився `10b7dc50b00f`).
- **Джерело:** сесія 2026-06-13, рішення користувача descope-нути фазу 2 до
  візуалізації. Опис майбутньої роботи — `design.md` зміни (заархівовано в
  `openspec/changes/archive/2026-06-13-add-calendar-timesheet/`) → «Deferred /
  Future work».

## D-017 — Екран «Проекти → TimeCamp»: BREAKING `/tc-projects`, дерево+попап, без авто-синку

- **Рішення (`rework-projects-screen`, реалізовано 2026-06-13):** під-вʼюху
  «Проекти → TimeCamp» переписано «по цеглинці» (стратегія D-016). Ключові вибори:
  - **BREAKING-контракт `GET /tc-projects`:** прибрано `start`/`end` і
    `entries_count` (проекти не залежать від періоду, D1); додано `parent_id`,
    `color`, `issue_name`/`issue_active` (резолв змапованої задачі **одним LEFT
    JOIN** `jr_issues ON tc_projects.issue_key = jr_issues.key`, soft-link за
    рядком-ключем, не FK, D2) і фільтр `active=active|inactive|all` (за
    `is_archived`, дефолт `all`, D4). Єдиний споживач — цей фронт, тож ламати
    контракт безпечно; бек+фронт деплояться разом.
  - **Похідна «активність» задачі (D3):** один хелпер `is_issue_active(status)`
    (`app/core/utils/issue_status.py`) звіряє вільний `status` із константним
    «done»-сетом (EN+UK: `done`/`closed`/`resolved`/`cancelled`/`готово`/
    `закрито`/`виконано`/`скасовано`/`вирішено`…), case-insensitive+trim;
    `None → active`. Спільний для `/tc-projects.issue_active` і
    `/jr-issues.active`. Прапор лише візуальний (тьмяність), сет — одна
    константа для розширення. `statusCategory` із Jira не зберігаємо (була б
    міграція) — відкладено.
  - **`GET /jr-issues` + пошук:** додано `q` (ILIKE `%q%` по `key`/`name`,
    `ORDER BY updated_at DESC`) і `limit` (**дефолт 10**, капується ≤50) для
    select-а з пошуком у попапі (D8). **Наслідок:** екран «Jira · Задачі»
    (`JiraView`) тепер явно просить `limit:50` (стеля), бо дефолт 10 інакше
    зрізав би список.
  - **Дерево на фронті (D5):** бек віддає плаский список; `lib/tree.ts`
    (`buildTree`/`flattenTree`) будує ліс із `parent_id` з **orphan-hoisting**
    (вузол із відфільтрованим/відсутнім батьком — у корінь, щоб нічого не
    зникло при фільтрі), стабільне сортування за `name`. Колір (`color`) —
    лише валідований hex (`^#?[0-9a-fA-F]{3,8}$`) як CSS-змінна, інакше
    нейтральний фолбек (без XSS, D6). Архівні вузли й закриті задачі — тьмяні.
  - **Попап замість інлайн-тогла (D7):** інлайн `is_sync`-тогл прибрано (ризик
    випадкового кліку). Налаштування синку — у `Sheet`-попапі (шестерня /
    подвійний клік): тогл + select задачі з пошуком. **Save заблоковано**, поки
    синк увімкнено, а задачу не обрано; вимкнення синку зберігає наявний
    `issue_key` (шлемо лише `{is_sync:false}`). `PATCH /tc-projects/{id}`
    повертає **той самий повний шейп**, що й list (резолвить `issue_name`), тож
    рядок замінюється цілком.
  - **Лінива загрузка по під-вʼюхах (D10):** прибрано авто-синк проектів і
    вантаження обох джерел батьком. Кожна під-вʼюха вантажить **своє** одним
    запитом, ідемпотентно: `/projects/timecamp` → **лише `GET /tc-projects`**;
    `GET /jr-projects` — **лише** при переході на вкладку Jira. Лічильники
    вкладок ліниві (badge лише для завантаженого джерела). Відкриття сторінки:
    **6→1** запит. Синк проектів — **окрема явна кнопка** «Синхронізувати
    проекти» (POST tc+jr → reload), ніколи не авто (D9).
- **Уточнення за фідбеком користувача (2026-06-13, та сама сесія):**
  - **Фільтр — за станом синку (`is_sync`), не `is_archived`, і клієнтський.**
    Значення `усі | у синку | не в синку` фільтрують **завантажений** список на
    клієнті (без re-fetch); архівні проекти завжди показані (лише тьмяні).
    Причина: на екрані конфігурації синку фільтр за архівністю давав
    порожні/однакові вибірки (майже всі проекти не архівні). Бекендний
    `active`(`is_archived`) param **лишається** як опційна можливість API, але
    UI його не використовує (вантажить усе, фільтрує локально).
  - **Швидке поле пошуку** по локальних даних (по `name`/`issue_key`/
    `issue_name`, case-insensitive), теж клієнтське, комбінується з фільтром.
  - **Тег задачі — одразу біля назви** проекту (а не у правій частині рядка),
    щоб звʼязок «проект → задача» читався без горизонтального сканування.
  - **Кнопка синку — лише сервіс активної під-вʼюхи** (`syncProjects(service)`,
    `service` деривується з `route.name`): на TimeCamp-вкладці синкає лише
    `POST /sync/timecamp/projects`, на Jira-вкладці — лише
    `POST /sync/jira/projects`, потім reload саме цього джерела; помилка → банер
    + кнопка в стані помилки.
- **Без alembic-міграції:** усі колонки (`parent_id`/`color`/`issue_key`/
  `status`) уже є; sync-рушій/моделі/схема БД не зачіпались (head лишився
  `10b7dc50b00f`).
- **Альтернативи:** *клієнтський резолв назв задач* (відкинули — тягнув би
  **всі** задачі, проти мети «не тягнути все»); *all-time `COUNT` замість
  прибраного `entries_count`* (відкинули — колонка зайва на екрані конфігурації);
  *eager-load обох джерел* (відкинули — два запити на одну сторінку);
  *центрована модалка* (відкинули — `Sheet` уже усталений патерн форм, як у
  `UsersView`).
- **Статус:** **реалізовано 2026-06-13**; `npm run build` (`vue-tsc`+`vite`) і
  бекенд-контракт (OpenAPI: без `entries_count`/`start`/`end`; є `active`/`q`/
  `limit`/`issue_*`) — чисті; `validate --strict` OK. Лишився **браузерний QA**
  (наживо) і git-commit.
- **Джерело:** сесія 2026-06-13, зміна `rework-projects-screen`
  (`openspec/changes/rework-projects-screen/`), три продуктові розвилки
  підтверджені користувачем (прибрати `entries_count`/date-фільтр; синк лише
  кнопкою; тьмянити архівні TC-проекти й закриті Jira-задачі).

## D-018 — Екран TimeCamp-записів: read-only з БД + явний синк; єдина «мова синку»

- **Рішення (`rework-timecamp-entries-screen`, реалізовано і заархівовано
  2026-06-23):** екран `/timecamp` переписано «по цеглинці» (стратегія D-016).
  Ключові вибори:
  - **Дані-екрани читають лише з локальної БД; синк — лише явною кнопкою**
    (узагальнення D9 з `rework-projects-screen` на записи). `onMounted` більше
    **не** робить авто-синк; новий `GET /tc-entries` віддає всі записи за період
    із локальної БД. Той самий патерн застосувати далі до `/jira` і `/tempo`
    окремими змінами (auto-memory `data-pages-no-auto-sync`; синк за розкладом —
    майбутній етап).
  - **`is_synced` — бінарно, scoped по `worker_key`:** запис synced, якщо існує
    `worklog_sync_task` у статусі `created`/`updated` (worklog у Tempo) для
    `worker_key` поточного користувача — через **EXISTS** у
    `TCEntriesDAO.list_with_sync_state` (чужий стан не протікає; `worker_key=NULL`
    → усе unsynced). `GET /tc-entries/untracked` лишився незмінним (під майбутній
    matching). **Без alembic-міграції** (head `10b7dc50b00f`).
  - **Серверна пагінація `{items, total}`** (`limit` 50/кап 200, `offset`),
    прецедент `GET /api-jobs`.
  - **Компактний `PeriodPicker` без зовнішньої бібліотеки** (одна кнопка →
    dropdown зі швидкими шаблонами всередину + ручні дати; поповер за патерном
    `Menu.vue`), переюзаний і тулбаром перегляду, і попапом синку. Готовий
    датапікер відкинуто — конфлікт із власною дизайн-системою + важка залежність
    (фронт тримає лише `vue`/`vue-router`/`pinia`).
- **Уточнення за фідбеком користувача (та сама сесія):**
  - **Єдина «мова синку» — спільні компоненти** `components/data/SyncState.vue`
    (крапка + підпис) і `SyncFilter.vue` (`Segmented` `All|Synced|Not synced`,
    канонічний `SyncTri`), **візуально ідентичні** на всіх дані-екранах. Переведено
    `/timecamp`, `/projects/timecamp`, `/projects/jira` (прибрано локальні
    `.tct__state`/`.tcst` і per-екранні `Segmented`-опції; мертві i18n-ключі
    видалено). Підписи — **англійською в обох локалях** (захардкоджено; єдиний
    короткий словник синку, без i18n-дивергенції). Причина: консистентність синку
    — суть продукту; раніше три екрани мали різний вигляд. Зафіксовано як
    обовʼязковий патерн у `systemPatterns.md` («Єдина мова синку») + auto-memory
    `sync-status-unified-ui`.
  - **Колонки `/timecamp`:** Проект → Задача → Опис → Дата → Час → Стан синку;
    задача (`issue_key`) — **окрема колонка**; дата — український формат
    `дд.мм.рррр` (`lib/format.ts → fmtDate`).
- **Capability-дельти (злиті в `openspec/specs/`, канонічні):** `api-sync-status`
  (ADDED `GET /tc-entries`), `frontend-data-tables` (MODIFIED «Екран TimeCamp» +
  ADDED «Єдине відображення стану синку»).
- **Статус:** реалізовано, браузерний QA підтверджено користувачем, заархівовано
  2026-06-23 (`archive/2026-06-23-rework-timecamp-entries-screen/`). `npm run build`
  + backend live smoke/DAO-перевірка + `validate --strict` — чисто.
